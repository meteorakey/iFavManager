<%= form_for(@twitter_auth) do |f| %>
  <% if @twitter_auth.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@twitter_auth.errors.count, "error") %> prohibited this twitter_auth from being saved:</h2>

      <ul>
      <% @twitter_auth.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
